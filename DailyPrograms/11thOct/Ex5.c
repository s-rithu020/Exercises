#include <stdio.h>

int main()
{
    void *ptr;
    char* ptr_c;
    int* ptr_i;
    float* ptr_f;

    printf("%d %d %d");

    sizeof(ptr_c);
    sizeof(ptr_i);
    sizeof(ptr_f);
    sizeof(ptr);
}